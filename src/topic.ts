/**
 * Append a message to a topic file
 * @param topic - the topic being discussed
 * @param message - the message being broadcast on the topic
 */
export const writeOnTopic = async (topic: string, message: string) => {
  try {
  } catch (e) {}
};
